import src.Bot as Bot

class VanillaBot(Bot):

    def find_closest_booth(self,board)
    '''
        find the closest booth in the neighbors. Makesure it's not visited already
        use BFS.
    '''
    def compute_step(self, board):
